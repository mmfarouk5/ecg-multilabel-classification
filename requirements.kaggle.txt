# Minimal dependencies for running experiments on Kaggle.
# Kaggle already provides core packages (torch, numpy, pandas, scipy, sklearn, matplotlib).
# Keep this list small to avoid dependency clashes with preinstalled notebook packages.

wfdb>=4.1,<5
iterative-stratification>=0.1.9,<0.2
optuna>=4,<5
